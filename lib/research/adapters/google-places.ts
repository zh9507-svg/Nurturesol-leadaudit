import type { DiscoveryBusiness, ResearchRunInput } from "@/types";

export async function discoverViaGooglePlaces(input: ResearchRunInput): Promise<DiscoveryBusiness[]> {
  void input;

  return [];
}
